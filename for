for(int i = 0; i= MAX; i++)
{
  strcpy(numeros2[i].nom,numeros3[i].nom);
  strcpy(numeros2[i].numero,numeros3[i].numero);
  strcpy(numeros2[i].prenom,numeros3[i].prenom);
  strcpy(numeros2[i].entsort,numeros3[i].entsort);
}
